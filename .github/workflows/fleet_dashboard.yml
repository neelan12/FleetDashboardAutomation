name: Deploy Workflow

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason for trigger"
        required: false
        default: "Triggered from n8n cloud"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print reason
        run: echo "Reason: ${{ github.event.inputs.reason }}"
